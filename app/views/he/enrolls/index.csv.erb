<% require 'fastercsv' -%>
<% csv_string = FasterCSV.generate do |csv|  
@subjects.each do |s|
	csv << [ s.childid, s.studyid, s.priority, s.last_name, s.first_name, s.outcome, s.outcome_date ]
end
end
-%>
<%= csv_string -%>
