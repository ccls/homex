<% content_for :head, stylesheet_link_tag('he_subjects') %>
<%= render 'he/subjects/subsearch' %>

<div class='header'>
<%= sort_link('ChildID') %>
<%= sort_link('StudyID') %>
<%= sort_link('Last Name') %>
<%= sort_link('First Name') %>
</div>
<div id='subjects'>
<%= render :partial => 'he/subjects/subject', :collection => @subjects %>
</div>

<%= will_paginate @subjects %>
