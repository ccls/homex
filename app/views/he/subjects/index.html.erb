<% content_for :head do %>
<%= stylesheet_link_tag('he_subjects') %>
<% end %>

<div class='header'>
<%= sort_link('ChildID') %>
<%= sort_link('StudyID') %>
<%= sort_link('Last Name') %>
<%= sort_link('First Name') %>
<%= sort_link('DOB') %>
</div>
<div id='subjects'>
<%= render :partial => 'he/subjects/subject', :collection => @subjects %>
</div>

<%= will_paginate @subjects %>

<%#= button_link_to "Create New Subject", new_subject_path %>

<%= render 'he/subjects/search' %>
