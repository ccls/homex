<% content_for :id_bar do %>
<%= link_to( 'Create', nil, {
	:id => "for_new_address",
	:class => :submitter
}) %>
<% end %>

<% form_for(@address,
	:url => subject_addresses_path(@subject)) do |f| %>
<%= render 'form', :f => f %>
<% end %>
