<% content_for :head do %>
<%= stylesheet_link_tag('subjects') %>
<% end %>

<h2>This is all still bogus development data.  Please feel free to click away and give me some feedback.</h2>

<%= will_paginate @subjects %>

<div id='subjects'>
<%= render :partial => 'subject', :collection => @subjects %>
</div>

<%= will_paginate @subjects %>

<%= button_link_to "Create New Subject", new_subject_path %>

<% form_tag(subjects_path, :method => :get) do %>
	<%= text_field_tag 'Something', nil, :id => nil %>
	<%= submit_tag 'Search Not yet implemented', :name => nil %>
<% end %>
