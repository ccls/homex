<% stylesheets('subject','contacts') %>
<% render 'subjects/submenu' %>

<% content_for :id_bar do %>
<%= link_to "edit", edit_subject_path(@subject) %>
<% end if current_user.may_edit_subjects? %>
<% render 'subjects/id_bar' %>

<% content_tag_for( :div, @subject ) do %>
<%= render 'subject_personal' %>
<%= render 'subject_parent' %>
<%= render 'subject_consent' %>
<%= render 'subject_outcomes' %>
<% end %><%# content_tag_for %>
