<% content_for :id_bar do %>
<%= link_to 'Save', nil, {
	:id => "for_#{dom_id(@subject,:edit)}",
	:class => :submitter
} %>
<% end %>

<% form_for(@subject, 
	:url => subject_path(@subject)) do |f| %>
<%= render 'form', :f => f %>
<% end %>
