<h3>New Subject</h3>

<% content_for :id_bar do %>
<%= link_to 'Create', nil, {
	:id => "for_new_subject",
	:class => :submitter
} %>
<% end %>

<% form_for(@subject, :url => subjects_path,
	:html => { :class => 'edit_subject' }) do |f| %>
<%= render 'form', :f => f %>
<% end %>
