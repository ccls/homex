<% stylesheets('base/jquery.ui.all.css') %>
<h4>GiftCard:</h4>
<% form_for(@gift_card,
		:html => { :class => 'confirm edit_gift_card' }
	) do |f| %>
	<%= f.error_messages %>
	<%= f.wrapped_text_field :vendor %>
	<%= f.wrapped_text_field :number %>
	<%= f.wrapped_date_text_field :issued_on %>

<%# I need to refine this to only include those that do not already have 
	plus this subject so the select includes it.
%>
<%# study_subjects = StudySubject.for_hx_followup({:paginate => false}) %>

	<%#= f.wrapped_collection_select :study_subject_id,
		study_subjects, :id, :to_s, :include_blank => true %>

	<p><%= f.submit( (@gift_card.new_record?)?'Create':'Update' ) %></p>
<% end %>
