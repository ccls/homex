<%# stylesheets(:edit_sample) %>
<% render 'subjects/submenu', :subject => @subject %>
<% render 'subjects/id_bar',  :subject => @subject %>

<%= f.error_messages %>

<%= f.wrapped_collection_select :sample_type_id, SampleType.all, 
	:id, :description, :include_blank => true,
	:label_text => 'SampleType' %>
