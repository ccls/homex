<% content_for :id_bar do %>
<%= link_to( 'Create', nil, {
	:id => "for_new_sample",
	:class => :submitter
}) %>
<% end %>
<% form_for( @sample,
	:url => study_subject_samples_path(@study_subject),
	:html => {:class => 'edit_sample'}) do |f| %>
<%= render 'form', :f => f %>
<% end %>
