<% stylesheets('edit_interview','base/jquery.ui.all.css') %>
<% render 'submenu' %>

<% id_bar_for(@subject) do %>
<%= link_to( 'Save', nil, {
	:id => "for_#{dom_id(@interview,:edit)}",
	:class => :submitter
}) %>
<% end %>

<% form_for(@interview,
	:url => interview_path(@interview),
	:html => { :class => 'edit_interview confirm' }) do |f| %>

<%= f.error_messages %>

<%= f.wrapped_text_field :began_on_string,
	:class => 'datepicker',
	:label_text => 'Began on:' %>

<%= f.wrapped_text_field :ended_on_string,
	:class => 'datepicker',
	:label_text => 'Ended on:' %>

<%= f.wrapped_collection_select :interviewer_id,
	Person.all, :id, :full_name, :include_blank => true, 
	:label_text => 'Interviewer:' %>

<%= f.wrapped_collection_select :language_id,
	Language.all, :id, :description, :include_blank => true, 
	:label_text => 'Language:' %>




<%= f.wrapped_text_field :respondent_first_name,
	:label_text => 'Respondent First Name:' %>

<%= f.wrapped_text_field :respondent_last_name,
	:label_text => 'Respondent Last Name:' %>


<% end %>
