<%= content_for :head, stylesheet_link_tag('user') %>

<fieldset id='main'>
<legend><%= @user.username %></legend>

<div class='person'><dl>
	<% %w( displayname mail username email ).each do |key| -%>
		<dt><%= key -%></dt>
		<dd><%= @user.attributes[key] -%></dd>
	<% end -%>
</dl>
</div>

<div id='roles'>
<%= link_to( image_tag(@user.gravatar_url), "http://gravatar.com",
	:target => 'new' )%>
<%= user_roles() %>
</div>
 
</fieldset> 
