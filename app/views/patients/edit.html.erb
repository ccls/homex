<% content_for :id_bar do %>
<%= link_to( 'Save', nil, {
	:id => "for_#{dom_id(@patient,:edit)}",
	:class => :submitter
}) %>
<% end %>

<% form_for(@patient,
	:url => study_subject_patient_path(@study_subject),
	:html => { :class => 'edit_patient confirm' }) do |f| %>
<%= render 'form', :f => f %>
<% end %>
