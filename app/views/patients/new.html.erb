<% content_for :id_bar do %>
<%= link_to( 'Create', nil, {
	:id => "for_new_patient",
	:class => :submitter
}) %>
<% end %>

<% form_for(@patient,
	:url => subject_patient_path(@subject),
	:html => { :class => 'new_patient edit_patient' }) do |f| %>
<%= render 'form', :f => f %>
<% end %>
