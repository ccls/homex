<% require 'fastercsv' -%>
<%# 
	adding :headers => true is supposed to set a flag in the file 
	somewhere marking it as having a header row, but I don't 
	think that it actually does.
%>
<%=	%w( childid studyid last_name first_name dob ).to_csv(:headers => true) -%>
<% @study_subjects.each do |s| -%>
<%=	[ s.childid, s.studyid, s.last_name, s.first_name, s.dob ].to_csv -%>
<% end -%>
