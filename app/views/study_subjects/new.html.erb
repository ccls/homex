<h3>New Subject</h3>

<% content_for :id_bar do %>
<%= link_to 'Create', nil, {
	:id => "for_new_study_subject",
	:class => :submitter
} %>
<% end %>

<% form_for(@study_subject, :url => study_subjects_path,
	:html => { :class => 'edit_study_subject' }) do |f| %>
<%= render 'form', :f => f %>
<% end %>
