<hr/>
<% if @study_subject.dust_kit %>
	<%= link_to( "Show dust kit package statuses", 
		study_subject_dust_kit_path(@study_subject), :class => 'button' ) %>
<% else %>
	<%= link_to( "Send Dust Kit", 
		new_study_subject_dust_kit_path(@study_subject), :class => 'button' ) %>
<% end %>
